name: Rubycritic

on: [push]

jobs:
  rubycritic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.7.6
      - run: bundle install
      - name: rubycritic
        run: bundle exec rubycritic
